{
  "name": "discord-bot-template",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development bun watch dist/index.js",
    "build": "npx tsc -p .",
    "start": "NODE_ENV=production node dist/index.js",
    "lint": "npx eslint . --ext .ts",
    "lint:fix": "npx eslint . --ext .ts --fix"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "discord.js": "^14.14.1",
    "tsc-watch": "^6.2.0"
  }
}
